/* eslint-disable no-console */

'use strict';

module.exports = {
    info: (message) => {
        console.log(message);
    },

    warn: (message) => {
        console.warn(message);
    },

    error: (message) => {
        console.error(message);
    }
};
